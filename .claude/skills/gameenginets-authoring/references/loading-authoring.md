# Loading, ValueResolver, and Spawning

GameEngineTS uses a declarative object-loading pipeline:

```text
Object descriptor
  -> ValueResolver resolves procedural @ values
  -> Object.load / Object.update / Object.execute runs
  -> GameObject receives normal component setup
```

This pipeline is shared by map entries, spawner entries, and future editor/AI-generated object descriptors.

## Object descriptors

Normal object construction fields go inside `Object`:

```json
{
  "Object": {
    "entity": "Debris",
    "load": {},
    "update": {},
    "execute": {}
  }
}
```

Spawner metadata stays outside `Object`:

```json
{
  "weight": 4,
  "groups": ["debris", "gameObjects"],
  "Object": {
    "entity": "Debris",
    "load": {}
  }
}
```

## ValueResolver commands

Procedural value commands begin with `@` and resolve before loading or updating.

### `@range`

```json
{ "@range": { "min": 0.07, "max": 0.10 } }
```

### `@intRange`

```json
{ "@intRange": { "min": 1, "max": 5 } }
```

### `@pointRange`

```json
{
  "@pointRange": {
    "x": { "min": 100, "max": 300 },
    "y": { "min": 400, "max": 500 }
  }
}
```

### `@pick`

```json
{ "@pick": ["PENCIL", "GLUE", "SCISSORS"] }
```

### `@weightedPick`

Array form:

```json
{
  "@weightedPick": [
    { "weight": 5, "value": "PENCIL" },
    { "weight": 2, "value": "GLUE" },
    { "weight": 1, "value": "SCISSORS" }
  ]
}
```

Object shorthand for simple string values:

```json
{
  "@weightedPick": {
    "PENCIL": 5,
    "GLUE": 2,
    "SCISSORS": 1
  }
}
```

### `@loadingRule`

Use `@loadingRule` for values supplied by the current component or loading context.

For `EntitySpawnerComponent`, the common form is:

```json
{ "@loadingRule": { "this": "spawnPosition" } }
```

This means: use the spawn position generated by the current spawner after `spawnArea` and distance filters are applied.

Future or game-specific components may expose type-level or local loading rules:

```json
{ "@loadingRule": { "BalloonComponent": "randomType" } }
```

```json
{ "@loadingRule": { "#BalloonBrain": "randomType" } }
```

## EntitySpawnerComponent

`EntitySpawnerComponent` is a runtime object factory. It should use the same `Object.load` and `Object.update` object-descriptor rules as map JSON.

### `spawnArea`

World rectangle:

```json
"spawnArea": {
  "space": "world",
  "rectangle": {
    "origin": "topLeft",
    "width": 1280,
    "height": 720,
    "padding": 90
  }
}
```

Local circle:

```json
"spawnArea": {
  "space": "local",
  "circle": { "radius": 500, "innerRadius": 100 }
}
```

### Static population

```json
"static": { "count": 10 }
```

### Dynamic population

```json
"dynamic": {
  "targetCount": 8,
  "burstCount": 2,
  "interval": 3
}
```

### Weighted object recipes

`entities[]` is a weighted object recipe table. Entries may use the same Entity with different load/update values.

```json
"entities": [
  {
    "weight": 4,
    "groups": ["debris", "gameObjects"],
    "Object": {
      "entity": "Debris",
      "load": {
        "position": { "@loadingRule": { "this": "spawnPosition" } },
        "scale": {
          "x": { "@range": { "min": 0.07, "max": 0.10 } },
          "y": { "@range": { "min": 0.07, "max": 0.10 } }
        },
        "rotation": { "@range": { "min": 0, "max": 6.283185307179586 } },
        "velocity": {
          "x": { "@range": { "min": -120, "max": 120 } },
          "y": { "@range": { "min": -120, "max": 120 } }
        }
      }
    }
  },
  {
    "weight": 1,
    "groups": ["debris", "gameObjects"],
    "Object": {
      "entity": "Debris",
      "load": {
        "position": { "@loadingRule": { "this": "spawnPosition" } },
        "scale": { "x": 0.16, "y": 0.16 },
        "rotation": { "@range": { "min": 0, "max": 6.283185307179586 } },
        "velocity": { "x": 20, "y": -20 }
      },
      "update": {
        "component": {
          "#Motion": {
            "options.torque": { "@range": { "min": -30, "max": 30 } }
          }
        }
      }
    }
  }
]
```

Prefer `LoadComponent` for normal setup. Use `Object.update` for one-off direct overrides.

## Do not add yet

Do not introduce game-state-dependent commands like `@condition` unless the query/execute return contract is explicitly defined and tested.
